package it.ingsw.revedia.controller;

import org.springframework.stereotype.Controller;

@Controller
public class InsertReview
{

	/*
	 * @RequestMapping(value = "/tag del form register", method =
	 * RequestMethod.POST) public void insertReview(@RequestParam("/tag corpo")
	 * String text, HttpSession session){
	 * 
	 * 
	 * String user = (String) session.getAttribute("nickname");
	 * 
	 * Review review = new Review(text,id,user); DAOFactory factory =
	 * DAOFactory.getDAOFactory(DAOFactory.POSTGRESQL); ReviewJDBC reviewJDBC =
	 * factory.getCommentoDAO(); reviewJDBC.insertReview(review); }
	 */

}
